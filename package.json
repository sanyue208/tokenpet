{
  "name": "desktop-token-pet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TokenPet — 桌面宠物，实时显示 GPT / Gemini / DeepSeek / 国内大模型的 token 消耗与余额 · 三月个人专用",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "proxy": "node tools/token-proxy.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
